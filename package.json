{
  "name": "x402-escrow",
  "version": "0.1.0",
  "private": true,
  "description": "x402 HTTP 402 Escrow Payment System — HTTP-native payments with escrow protection",
  "scripts": {
    "dev": "bun --watch src/server/index.ts",
    "start": "bun src/server/index.ts",
    "build:contracts": "cd contracts && forge build",
    "test:contracts": "cd contracts && forge test",
    "sync-abi": "bun scripts/sync-abi.ts"
  },
  "dependencies": {
    "express": "^4.21.0",
    "better-sqlite3": "^11.0.0",
    "viem": "^2.21.0",
    "dotenv": "^16.4.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/express": "^5.0.0",
    "@types/better-sqlite3": "^7.6.0",
    "@types/uuid": "^10.0.0",
    "@types/node": "^22.0.0"
  }
}
